import React from "react";

import { MemberCenter } from "@containers";

const MemberCenterScreen = ({navigation}) => {
    return (
        <MemberCenter navigation={navigation}/>
    );
}

export default MemberCenterScreen

